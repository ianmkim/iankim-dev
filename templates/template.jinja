<!DOCTYPE html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes" />
  <meta name="author" content="{{author}}" />
  <meta name="dcterms.date" content="{{date}}" />
  <title>{{title}}</title>
  <link rel="stylesheet" href="/assets/index.css" />
  {% block head %}
  {% endblock %}
</head>
<body>
<table class="header">
  <tr>
    <td colspan="2" rowspan="2" class="width-auto">
      <h1 class="title">{{header.title}}</h1>
      <span class="subtitle">{{header.subtitle}}</span>
    </td>
    <th>Version</th>
    <td class="width-min">{{header.version}}</td>
  </tr>
  <tr>
    <th>Updated</th>
    <td class="width-min"><time style="white-space: pre;">{{header.date}}</time></td>
  </tr>
  <tr>
    <th class="width-min">Author</th>
    <td class="width-auto"><cite>{{header.author}}</cite></td>
    <th class="width-min">License</th>
    <td>MIT</td>
  </tr>
</table>
<div class="abstract">
<div class="abstract-title">{{abstract_title}}</div>
</div>
  {% block body %}{% endblock %}
  <script src="/assets/index.js"></script>
</body>
</html>
